<h1>Restaurants</h1>
<%= link_to "new restaurant", new_restaurant_path %>

<div class="container">
  <ul>
  <% @restaurants.each do |restaurant| %>
  <li>
  <%= restaurant.name %>

  <%= link_to "See more", restaurant_path(restaurant), class: "btn btn-primary" %>
  </li>
<% end %>
</ul>
</div>
